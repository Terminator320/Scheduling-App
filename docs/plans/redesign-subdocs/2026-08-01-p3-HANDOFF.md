# P3 — Clients HANDOFF

**Status: CODE-COMPLETE 2026-08-01** on branch `redesgin`.
`flutter analyze` → 0 issues · `flutter test` → **1255 passing** ·
`cd functions && npm run lint` clean · `npm test` → **684 passing**.

Two things remain, both outside the code: the **backend deploy** (§6) and the
**on-device pass**.

---

## 1. What shipped

| Task | What landed | Commit |
| --- | --- | --- |
| T1 | `ClientType` enum; `ClientRecord` gains `type`, `tags`, `accessNotes`, `onSiteManager`, `billingTerms`, `autoInvoice`, `jobCount`; `TextLimits` caps; matching `isValidClientData` caps; `jobCount` rejected on both client write paths | `703c081` |
| T2+T3 | **Archive dropped, delete withdrawn** (owner decision) — see §2 | `4df40e9` |
| T5 | `recountClientJobs` + pure `clientsToRecount`, exported from `index.js`; 8 jest tests | `43b259f` |
| T6 | New-client sheet rebuilt on `FormSheetFrame` — Cancel · New client · **Add**, WHO / REACH THEM / SITE, type chips, access notes, *Add and book a job* | `9fdad4b` |
| T7 | `AddEventSheet(initialClient:)` + `showAddEventPopup(initialClient:)`; both add-client call sites honour `AddClientNext.bookJob` | `dd5d7c7` |
| T8 | `EditClientSheet` (CLIENT / CONTACT / SITE / BILLING, read-only SINCE, tags editor); inline `ClientEditForm` deleted; mobile consolidation | `f7774ac` |
| T9 | Client detail on `KeyValuePanel`; Edit pill; Call · Directions · **Book job** tiles | `15e0b8f` |
| T10 | Client row: address subtitle, type+tag chips, right-aligned mono job count; `ListItemTile.subtitleExtra` added | `f16a19d` |
| T11 | 20-case scale sweep, docs, this handoff | *this commit* |

---

## 2. Deviations — do NOT "fix" these back

### 2.1 A client can never be removed (owner decision, 2026-08-01)

The plan shipped archive **and** kept the existing delete. The owner chose
neither, which also **withdrew a shipped feature**.

- **Why delete went:** deleting a client orphaned their history — past
  appointments keep the denormalized `clientName` but lose the `clientId` link.
- **Why archive never arrived:** it was the safe alternative, but it is what
  paid for the whole pagination rework. Archived docs must be filtered **in
  Dart** (pre-existing and Wave-imported docs have no `archived` field, and
  Firestore excludes docs missing a filter field). Filtering after the query
  shortens a page the server actually filled, which breaks the list's
  `items.length < pageSize` end-of-list test — one archived doc in a full page
  would truncate the list permanently.
- **Net effect on the code:** `fetchClientsPage` keeps its plain
  `Future<List<ClientRecord>>` and `_pageBoundaryNames` cursor exactly as they
  were pre-P3. `ClientPage`, `ClientCursor`, `hasMore`, `fetchArchivedClients`,
  `archivedClientsProvider`, `ClientListFilter`, `ClientsFilterBar` and
  `setArchived` were all built, then removed unbuilt-back. Do not resurrect any
  of them piecemeal.
- **The rule to remember:** if any future feature filters the clients page after
  the query returns, the page shape MUST change with it — a page object carrying
  the raw server-page size and a raw cursor. That is written up in CLAUDE.md.
- **Accepted cost, stated plainly:** the clients list and the booking picker now
  grow monotonically. A one-off customer stays in both forever. `deleteClient`
  is gone from the repository, the controller, the detail view and
  `firestore.rules` (`allow delete` withdrawn). The **Admin SDK bypasses rules**,
  so console/support cleanup still works.

### 2.2 Mobile consolidation replaces a migration

Owner change 5 removed `mobile` from the edit sheet. A removed field must not
strand a stored value, so `EditClientSheet._save` promotes `mobile` into `phone`
when `phone` is empty and clears `mobile` either way — on every save. The fleet
heals as clients are edited; there is no migration script and none is needed.
Pinned by three tests (`edit_client_sheet_test.dart`, group
`mobile consolidation`), one per row of the decision.

### 2.3 Smaller ones

| Plan says | We ship | Why |
| --- | --- | --- |
| `showEditClientSheet` returns a sealed `EditClientOutcome` | Returns `ClientRecord?` | The sealed family existed to distinguish cancelled / saved / **gone**. With no archive and no delete there is no third state, so null unambiguously means cancelled. |
| Tests built on `FakeFirebaseFirestore` | Existing mocktail harness | `fake_cloud_firestore` is not a dependency and nothing in the repo uses it. The plan itself said to match the existing harness. |
| `ClientFormActivity { isSaving, isDeleting }` | Bare `bool` | With `isDeleting` gone, a one-field immutable wrapper with `copyWith`/`==`/`hashCode` was ceremony for one bool across 4 read sites. |
| New-client sheet gets a lean single address line | Full `ClientAddressSection` + `noFixedAddress` | Owner change 3. The **same sheet** opens from the appointment form's inline add-client path, where a booking needs a split address. This is why `validateNew` was cancelled and `ClientFormValidator.validate` is reused. |

---

## 3. Bugs and traps found while building

1. **`_applyLocalWrite`'s null branch was delete-only.** Removing delete left it
   unreachable; the parameter is now non-nullable so the dead case can't rot.
2. **The l10n hook only fires on Edit/Write tool calls.** ARB edits made by
   script need a manual `flutter gen-l10n`. Bit this twice.
3. **`find.text('Save')` is ambiguous on the client detail.** The read-only body
   carries a "Save to contacts" tile whose label also renders `Save`, behind the
   edit sheet. Tests scope it via `find.descendant(of: find.byType(EditClientSheet), …)`.
4. **`ListItemTile.subtitle` is single-line by contract**, so the chip row needed
   a new general `subtitleExtra` slot rather than a `Column` crammed into
   `subtitle`. P4's team rows will want the same slot.
5. **A stale root-level `test/client_tile_test.dart`** duplicated the feature-scoped
   tile test and asserted the phone was the subtitle. Corrected in place rather
   than deleted — its tap / avatar / overflow cases are still unique coverage.

---

## 4. Invariants established (now in CLAUDE.md)

- A client is never removed — no delete, no archive; and *why* the pagination
  shape depends on that staying true.
- `jobCount` is recomputed absolutely, never incremented (`retry: true` would
  double-count), fires only when `clientId` changes, and writes with `update()`
  so a client removed out-of-band is never resurrected as a count-only stub.
- `toMap` must never emit `waveCustomerId` / `wave` / **`jobCount`**.
- `mobile` self-heals into `phone` on every edit save.

---

## 5. What P3 hands P4 (team)

- **`ListItemTile.subtitleExtra`** — the general below-subtitle slot, built for
  the client row's chips; the team roster rows want the same shape.
- **`FormSheetFrame` is now proven on a long form** (the edit sheet is the
  biggest in the app). Employee sheets can migrate off `FormSheetScaffold` the
  same way; `FormSheetScaffold` survives only for the employee sheets now.
- **`ClientTypeChips`** is the reference pattern for an optional single-select
  chip row that clears on re-tap and has **no** side effects on other fields.
- **The mono section-label helper** is duplicated as a private `_SectionLabel` in
  both client sheets and the appointment form. Three copies is the point at which
  it should become shared — P4 adding a fourth should promote it.

---

## 6. Still open

1. **Backend deploy — REQUIRED before any job count renders.**
   ```bash
   firebase deploy --only functions,firestore:rules
   ```
   `recountClientJobs` is a new function and `firestore.rules` changed (new field
   caps, `jobCount` rejection, `allow delete` withdrawn).
   `firestore.indexes.json` is **unchanged** — the `where('clientId','==',x)`
   count is served by the automatic single-field index — so omit
   `firestore:indexes`, and **never pass `--force`** (it deleted all five
   production TTL policies once).
2. **On-device pass** (Mac/iPhone), still pending from P1+P2 as well. P3 things
   to look at specifically: the two sheets at XL text, the Book-job tile
   round-trip, and *Add and book a job* sequencing (the add sheet must fully pop
   before the appointment sheet opens — they must never stack).
3. **Backfill is lazy by design.** Existing clients render no job count until
   their next appointment write. That is expected, not a bug.
