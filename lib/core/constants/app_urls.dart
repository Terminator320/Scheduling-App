/// External URLs the app links out to.
///
/// [privacyPolicy] is surfaced as a tappable tile in Settings → Legal and is
/// **required for App Store submission**. Replace the value below with the
/// public URL where the privacy policy is hosted.
/// Source document: `docs/legal/privacy-policy.html`.
class AppUrls {
  const AppUrls._();

  /// Public privacy-policy page. Update this to the hosted URL before release.
  static const String privacyPolicy =
      'https://gvogas.github.io/es-pro-legal/';
}
